{
  "name": "openbox-approver",
  "version": "0.1.0",
  "private": true,
  "description": "Native macOS tray app for reviewing OpenBox approvals. Tray-only, no JS frontend; src-tauri/ is the entire app.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
